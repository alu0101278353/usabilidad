import React from 'react'
import imagen from '../images/imgFaqs.png'

export const ImagenCartaFaqs = () => {
  return (
    <img src={imagen} alt='Logo Preguntas' className='imagen-carta' aria-label='Imagen de guia'/>
  )
}